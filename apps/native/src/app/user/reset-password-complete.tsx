import {Layout} from '~/layout/Layout';
import {ResetPasswordComplete} from '~/user/ResetPasswordComplete';

export default function () {
	return <Layout Content={ResetPasswordComplete} title="Paswword has been succesefully changed!" />;
}
